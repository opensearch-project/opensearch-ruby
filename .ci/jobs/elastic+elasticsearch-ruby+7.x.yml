---
- job:
    name: elastic+opensearch-ruby+7.x
    display-name: 'elastic / opensearch-ruby # 7.x'
    description: Testing the opensearch-ruby 7.x branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/7.x
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
