---
- job:
    name: elastic+opensearch-ruby+master
    display-name: 'elastic / opensearch-ruby # master'
    description: Testing the opensearch-ruby master branch.
    parameters:
    - string:
        name: branch_specifier
        default: refs/heads/master
        description: the Git branch specifier to build (&lt;branchName&gt;, &lt;tagName&gt;,
          &lt;commitId&gt;, etc.)
